CMAKE_MINIMUM_REQUIRED (VERSION 2.6)

SET (CMAKE_MODULE_PATH $ENV{OPENCV_DIR})

FIND_PACKAGE(OpenCV REQUIRED)

PROJECT(temporalseg)

ADD_EXECUTABLE(temporalseg src/main.cpp) 

TARGET_LINK_LIBRARIES (temporalseg ${OpenCV_LIBS})